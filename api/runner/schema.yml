domain: dev.miren.runner
version: v1alpha

enums:
  invite_status:
    values: [pending, claimed, revoked, expired]

kinds:
  runner_invite:
    code_hash:
      type: string
      doc: SHA-256 hash of the join code (code itself is not stored)
      indexed: true

    labels:
      type: label
      doc: Labels to apply to the runner when it joins
      many: true

    expires_at:
      type: time
      doc: When the invite expires

    status:
      type: enum
      enum: invite_status
      encoding: ref
      doc: Status of the invite
      indexed: true

    claimed_by:
      type: string
      doc: Runner ID that claimed this invite

    claimed_at:
      type: time
      doc: When the invite was claimed

    created_at:
      type: time
      doc: When the invite was created

    reusable:
      type: bool
      doc: Whether this invite can be used multiple times

    name:
      type: string
      doc: Human-readable name for audit and management
      indexed: true

    enrollment_count:
      type: int
      doc: Number of runners that have joined using this invite
