kind: dev.miren.compute/sandbox
version: v1alpha
metadata:
  name: nginx
  labels:
    - app=nginx
spec:
  labels:
    - app=nginx

  port:
    - port: 80
      name: http
      type: http

  container:
    - image: docker.io/library/nginx:latest
      name: nginx
