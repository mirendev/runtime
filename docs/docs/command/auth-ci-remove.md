---
title: "miren auth ci remove"
sidebar_label: "auth ci remove"
description: "Remove a CI authentication binding"
---

# miren auth ci remove

Remove a CI authentication binding

## Usage

```bash
miren auth ci remove <id> [flags]
```

## Arguments

- `id` — ID of the CI authentication binding to remove

## Flags

- `--cluster, -C` — Cluster name
- `--config` — Path to the config file

## Global Options

- `--options` — Path to file containing options
- `--server-address` — Server address to connect to (default: `127.0.0.1:8443`)
- `--verbose, -v` — Enable verbose output

## See also

- [`miren auth ci`](./auth-ci.md)
