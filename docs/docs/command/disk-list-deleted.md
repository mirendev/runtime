---
title: "miren disk list-deleted"
sidebar_label: "disk list-deleted"
description: "List deleted disks available for recovery"
---

# miren disk list-deleted

List deleted disks available for recovery

## Usage

```bash
miren disk list-deleted [flags]
```

## Flags

- `--cluster, -C` — Cluster name
- `--config` — Path to the config file
- `--format` — Output format (text, json) (default: `text`)
- `--json` — Shorthand for --format json

## Global Options

- `--options` — Path to file containing options
- `--server-address` — Server address to connect to (default: `127.0.0.1:8443`)
- `--verbose, -v` — Enable verbose output

## See also

- [`miren disk`](./disk.md)
